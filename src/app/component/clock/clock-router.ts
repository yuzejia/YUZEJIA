// 模块内的路由设置

import {ModuleWithProviders} from '@angular/core';
import {RouterModule, Routes} from '@angular/router';


const routes: Routes = [
];

export const clockRouter: ModuleWithProviders = RouterModule.forChild(routes);
